// Home.jsx — the full Home page
// Right now it only has HeroSection
// Later we will add RecentGames, StatsBar etc. here

import HeroSection from '../components/home/HeroSection'

function Home() {
    return (
        <div>
            <HeroSection />
        </div>
    )
}

export default Home